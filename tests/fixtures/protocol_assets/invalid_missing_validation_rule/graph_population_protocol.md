# Minimal Fixture Graph-Population Protocol

Status: fixture

This fixture is domain agnostic. It exists only to prove that a generated bundle
has the control surfaces required before population starts.

Graph pair:

```text
fg -> tg
```

No graph facts are populated by this fixture.
